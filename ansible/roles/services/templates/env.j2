# ============================================================
# Homelab .env — generated by Ansible from Vault
# DO NOT EDIT MANUALLY — changes will be overwritten on next
# ansible-playbook site.yml --tags services
# Edit secrets via: ansible-vault edit ansible/vault.yml
# ============================================================

# --- Pi-hole -------------------------------------------------
PIHOLE_PASSWORD={{ pihole_password }}

# --- Tailscale -----------------------------------------------
TS_AUTHKEY={{ ts_authkey }}

# --- Telegram bot --------------------------------------------
TELEGRAM_BOT_TOKEN={{ telegram_bot_token }}
TELEGRAM_CHAT_ID={{ telegram_chat_id }}

# --- Cloudflare ----------------------------------------------
CF_API_TOKEN={{ cf_api_token }}

# --- Backup --------------------------------------------------
BACKUP_CRON={{ backup_cron | default('0 2 * * *') }}
BACKUP_RETENTION_DAYS={{ backup_retention_days | default(14) }}
